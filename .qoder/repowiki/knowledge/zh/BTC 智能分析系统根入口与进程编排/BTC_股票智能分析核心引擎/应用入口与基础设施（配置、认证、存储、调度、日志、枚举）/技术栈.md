Python 标准库 + `sqlalchemy`（ORM + SQLite 方言）+ `schedule`（轻量定时任务）+ `python-dotenv`（.env 加载）+ `pandas`（存储层数据转换）；密码哈希使用 `hashlib.pbkdf2_hmac` (SHA256, 100_000 次迭代) 与 `hmac.compare_digest` 常量时间比较；会话签名使用 `secrets.token_urlsafe` + HMAC-SHA256。