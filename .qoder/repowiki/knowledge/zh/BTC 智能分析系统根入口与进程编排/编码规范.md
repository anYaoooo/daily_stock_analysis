- 所有子模块通过 src.* 命名空间暴露接口，主程序采用延迟导入避免冷启动开销
- 环境变量统一通过 src.config.setup_env() 加载 .env 文件，USE_PROXY 控制代理注入
- 日志通过 src.logging_config.setup_logging() 统一初始化，按 log_prefix 区分不同进程
- CLI 参数通过 argparse 集中定义，--debug/--dry-run/--schedule/--serve 等标志贯穿各入口脚本
- 配置文件遵循 .env 键值对格式，关键变量在 README.md 中集中说明