- 所有可写目录（/app/data、/app/logs、/app/reports、/home/dsa/.longbridge）在 entrypoint 中以数组形式集中声明，便于统一权限修复逻辑。
- 环境变量通过 docker-compose 的 env_file 引用 ../.env 注入，避免在 compose 文件中硬编码敏感配置。
- 卷挂载遵循宿主机路径映射到 /app/* 的约定，只读目录（如 strategies）显式标注 :ro 标志。
- 服务通过 YAML anchor 复用公共配置（build context、restart、volumes、logging、deploy resource limits），减少重复。