- 所有用户可见文案通过 `labels.*_label` / `labels.*_heading` 等键从上下文注入，模板内不硬编码中文文案。
- 对可选的 dashboard 字段统一采用 `dashboard.get('key', {})` 或 `or {}` 防御式取值，避免 KeyError。
- 长文本输出统一使用切片截断（如 `[:60]`、`[:50]`、`[:80]`）防止消息过长。
- 每条股票条目以 `e.signal_emoji` + `e.stock_name` + `e.result.code` 作为标识前缀，保持三模板一致的展示顺序。
- LLM 模型信息通过 `show_llm_model and models_used` 条件块统一在模板末尾追加，不侵入正文结构。