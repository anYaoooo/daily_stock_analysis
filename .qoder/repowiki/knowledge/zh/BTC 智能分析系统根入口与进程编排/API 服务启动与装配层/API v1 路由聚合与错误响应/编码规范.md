- 每个业务子模块通过 `include_router` 以 `prefix="/<模块名>"` 和 `tags=["<模块名>"]` 的形式注册到 v1 主路由中，保持路径与标签命名一致。
- 错误响应统一通过 `errors.py` 中的 `error_body`、`api_error`、`error_json_response` 三件套构造，返回固定包含 `error`、`message`、可选 `detail` 字段的字典结构。
- 模块仅暴露必要的符号（如 `__all__ = ["api_v1_router"]`），避免泄露内部实现细节。