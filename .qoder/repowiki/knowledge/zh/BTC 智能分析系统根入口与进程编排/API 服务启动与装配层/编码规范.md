- 应用装配统一通过 `create_app()` 工厂函数完成，默认实例在模块末尾以 `app = create_app()` 导出供 uvicorn 使用。
- 中间件通过独立的 `add_*_middleware(app)` 函数注册，保持路由与横切关注点分离。
- 路由按版本（v1）和领域（endpoints/*）分层组织，由 router.py 统一聚合并挂载到 `/api/v1` 前缀。
- 对外暴露的响应体使用 Pydantic schema（位于 `api/v1/schemas/`），健康检查等公共模型放在 `common.py`。