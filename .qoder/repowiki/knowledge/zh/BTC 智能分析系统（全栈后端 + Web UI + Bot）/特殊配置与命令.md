多入口运行方式：`python main.py --serve-only` 仅启动 API；`python main.py --serve` 同时执行定时分析任务；`python main.py --webui` 启动 Web 界面；`python server.py` 使用 uvicorn 直接启动；`python main.py --schedule` 启用北京时间 08:00 日线分析与每小时 05 分小时线分析；前端需先在 `apps/dsa-web` 下执行 `npm install && npm run build` 生成 `static/` 资源。