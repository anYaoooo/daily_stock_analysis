定时模式：`docker-compose -f ./docker/docker-compose.yml up -d`；FastAPI 服务：`docker-compose -f ./docker/docker-compose.yml up -d server`；同时启动两者：`docker-compose -f ./docker/docker-compose.yml up -d analyzer server`。需提前准备 `../.env` 配置文件，Longbridge OAuth token 缓存需先创建 `../longbridge_tokens` 目录。