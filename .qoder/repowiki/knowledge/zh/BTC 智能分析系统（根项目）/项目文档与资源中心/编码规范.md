- 重要文档均提供中英文对照版本，英文文件名以 `_EN` 后缀命名（如 README_EN.md、INDEX_EN.md、DEPLOY_EN.md）。
- API 契约以单一 OpenAPI JSON 文件集中管理，所有 HTTP 接口的路径、参数、响应 schema 均以 `$ref` 引用组件定义，禁止在文档中重复描述接口细节。
- Bot 平台配置采用「Markdown 步骤说明 + 配套截图」的组合方式，截图与说明文件放在同一 `bot/` 目录下便于同步更新。
- 静态资源按用途分目录存放（`assets/` 放通用图片/GIF、`assets/dsa_vi/` 专放应用图标与 PSD/AI 源文件），避免散落在文档正文中。