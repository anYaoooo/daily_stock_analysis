- 所有仓库级协作规范以 `AGENTS.md` 为单一权威源，`.github/copilot-instructions.md` 与其保持同步并通过引用而非复制内容。
- CI 依赖通过 `requirements-ci.txt` 使用 `-r ../requirements.txt` 继承根依赖，再追加测试与 lint 工具（flake8、pytest），避免重复声明。
- Release 变更日志按标签分类组织，统一排除 dependabot 与 github-actions[bot] 作者，确保自动化贡献不污染用户可见变更。
- PR 模板强制要求提供可复现验证命令、兼容性风险评估与明确回滚方案，且对涉及 EXTRACT_PROMPT 的变更要求展开粘贴完整 prompt。
- 文档更新遵循 README 仅保留首页级信息、详细实现写入 `docs/*.md` 的约定，CHANGELOG 的 `[Unreleased]` 使用扁平单行格式以减少合并冲突。