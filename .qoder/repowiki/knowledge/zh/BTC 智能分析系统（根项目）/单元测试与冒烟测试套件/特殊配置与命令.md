默认 pytest 运行会跳过所有标记为 `legacy_stock` 的 A 股退役测试；要包含它们需设置环境变量 `DSA_INCLUDE_LEGACY_STOCK_TESTS=1`。Longbridge 冒烟脚本需先复制 `.env.example` 到 `.env` 并填入 `LONGBRIDGE_*` 凭据，然后直接 `python tests/longbridge_live_smoke.py [TICKER] [--lb-app-key ... --lb-app-secret ... --lb-access-token ...]` 执行。