Python 3（argparse + pathlib + subprocess + dotenv + pandas + pypinyin）、Bash（set -euo pipefail）、PowerShell（$ErrorActionPreference=Stop）、PyInstaller（打包后端）、Vite（前端静态资源）、pytest（离线测试）、flake8/py_compile（语法检查）、Electron/NSIS（桌面端更新元数据 latest.yml）。